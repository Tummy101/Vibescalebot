app = "vibescalebot"
primary_region = "jnb"

[build]
  dockerfile = "Dockerfile"

[env]
  DATABASE_NAME = "/data/vibescale.db"

[mounts]
  source = "vibescalebot_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
